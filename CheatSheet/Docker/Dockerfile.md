It contains steps to run our application.
[Reference](https://docs.docker.com/engine/reference/builder/)
You can use  VS Code extension to generate the *Dockerfile*, *.dockerignore* and *docker-compose.yml*
