## Host Group

```
ansible_project
 |__host_vars
    |__ip_address/domain_address.yml
 |__roles
	|__web_server
	   |__files    # directory if any file is mentioned in taskbook
	   |__tasks
	   |__handlers
```
- Make `host_vars` directory
```sh
mkdir host_vars
cd host_vars
```
- Make file `ip_address/domain_address.yml`
```yml
# host variables ubuntu
apache_package_name: apache2
apache_service: apache2
php_package_name: libapache2-mod-php
```
- Make use of variable in taskbook
```yml
 - name: install apache2 package (Ubuntu)
   tags: apache,apache2,ubuntu
   package:
     name:
       - "{{apache_package_name}}"
       - "{{libapache2-mod-php}}"
     state: latest
 
 - name: start and enable httpd (CentOS)
   tags: apache,centos,httpd
   service:
     name: "{{apache_package_name}}"
     state: started
     enabled: yes
   when: ansible_distribution == "CentOS"
 
 - name: change e-mail address for admin
   tags: apache,centos,httpd
   lineinfile:
     path: /etc/httpd/conf/httpd.conf
     regexp: '^ServerAdmin'
     line: ServerAdmin somebody@somewhere.net
   when: ansible_distribution == "CentOS"
   register: httpd
 
 - name: restart httpd (CentOS)
   tags: apache,centos,httpd
   service:
     name: httpd
     state: restarted
   when: httpd.changed    
 
 - name: copy html file for site
   tags: apache,apache,apache2,httpd
   copy:
     src: default_site.html
     dest: /var/www/html/index.html
     owner: root
     group: root
     mode: 0644
```

## Handler

- Make directory under role `roles/web_server`
```sh
 mkdir handlers
 cd handlers
```
- Make `main.yml`
```yml
 - name: restart_apache
   tags: apache,centos,httpd
   service:
     name: "{{apache_service}}"
     state: restarted
```
- Change 
```yml
 - name: install apache2 package (Ubuntu)
   tags: apache,apache2,ubuntu
   package:
     name:
       - {{apache_package_name}}
       - {{libapache2-mod-php}}
     state: latest
 
 - name: start and enable httpd (CentOS)
   tags: apache,centos,httpd
   service:
     name: {{apache_package_name}}
     state: started
     enabled: yes
   when: ansible_distribution == "CentOS"
 
 - name: change e-mail address for admin
   tags: apache,centos,httpd
   lineinfile:
     path: /etc/httpd/conf/httpd.conf
     regexp: '^ServerAdmin'
     line: ServerAdmin somebody@somewhere.net
   when: ansible_distribution == "CentOS"
   notify: restart_apache                    # Changed here

# ---- removed restart here -----

 - name: copy html file for site
   tags: apache,apache,apache2,httpd
   copy:
     src: default_site.html
     dest: /var/www/html/index.html
     owner: root
     group: root
     mode: 0644
```