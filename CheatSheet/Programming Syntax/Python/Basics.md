
### Reserved words in Python

```python
and       del       global      not       with
as        elif      if          or        yield
assert    else      import      pass
break     except    in          raise
class     finally   is          return
continue  for       lambda      try
def       from      nonlocal    while  
```


### Determining variable type with `type()`

Python's built-in `type()` function. Common data types include:
* **int** (for integer)
* **float**
* **str** (for string)
* **list**
* **tuple**
* **dict** (for dictionary)
* **set**
* **bool** (for Boolean True/False)


### Naming the variable in Python

The names you use when creating these labels need to follow a few rules:

1. Names can not start with a number.
2. There can be no spaces in the name, use _ instead.
3. Can't use any of these symbols :'",<>/?|\()!@#$%^&*~-+
4. It's considered best practice (PEP8) that names are lowercase.
5. Avoid using the characters 'l' (lowercase letter el), 'O' (uppercase letter oh), 
   or 'I' (uppercase letter eye) as single character variable names.
6. Avoid using words that have special meaning in Python like "list" and "str"



```python
# Comment is done 

# To print 
print('Hi')

# Use of input, it always returns string
speed = input('Enter your speed: ')

```

### Arithmetic Operators

```python
# Addition
2+1

# Subtraction
2-1

# Multiplication
2*2

# Division
3/2    # this will always return floating number

# Floor Division
7//4   # will give the quotient 

# Modulo
7%4   # will give the reminder

# Powers
2**3

# Can also do roots this way
4**0.5
```

### Assignment Operator

`a = 5`   Python lets you add, subtract, multiply and divide numbers with reassignment using `+=`, `-=`, `*=`, and `/=`.

