## Get details on the hostname
```sh
hostnamectl
```
## Change Hostname
```sh
hostnamectl set-hostname new_hostname
```

or 

edit `/etc/hostname` and change `current_name` to `new_name`
and edit `/etc/hosts` and change `current_name` to `new_name`